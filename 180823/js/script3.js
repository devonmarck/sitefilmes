var numero = prompt("escreva o numero para multiplicar")

for(contador = 1; contador <= 10; contador = contador +1){
    
    var resultador = numero * contador;
    alert(numero + " x " + " = " + resultado);
}
